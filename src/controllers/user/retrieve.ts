import { Request, Response, NextFunction } from "express";

// 전체 회원 조회
export const getAllUserList = (req: Request, res: Response) => {};

// 전체 회원 구독 목록 조회
export const getAllUserSubsList = (req: Request, res: Response) => {};

// 전체 회원 주문 목록 조회
export const getAllUserOrderList = (req: Request, res: Response) => {};
