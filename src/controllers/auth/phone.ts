import { Request, Response, NextFunction } from "express";

// 휴대폰 인증번호 전달
export const sendAuthCode = (req: Request, res: Response) => {};

// 휴대폰 인증번호 확인
export const checkAuthCode = (req: Request, res: Response) => {};
