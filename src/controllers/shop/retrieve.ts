import { Request, Response, NextFunction } from "express";

// 전체 가게 목록 조회
export const getAllShopList = (req: Request, res: Response) => {};

// 동네 가게 목록 조회
export const getAroundShopList = (req: Request, res: Response) => {};
